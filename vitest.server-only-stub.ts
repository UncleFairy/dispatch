// Test stub for the `server-only` import guard: in real builds it throws if
// pulled into a Client Component bundle, but that check is meaningless under
// Vitest's plain Node resolution, so tests alias it to this no-op.
export {};
